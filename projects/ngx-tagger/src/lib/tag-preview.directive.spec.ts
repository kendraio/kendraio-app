import { TagPreviewDirective } from './tag-preview.directive';

describe('TagPreviewDirective', () => {
  it('should create an instance', () => {
    const directive = new TagPreviewDirective();
    expect(directive).toBeTruthy();
  });
});
